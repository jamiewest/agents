name: agents_workspace
publish_to: none

environment:
  sdk: ^3.11.5

workspace:
  - packages/agents
  - packages/agents_flutter

dependency_overrides:
  extensions:
    path: ../extensions/packages/extensions
